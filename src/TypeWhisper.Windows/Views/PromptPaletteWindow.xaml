<Window x:Class="TypeWhisper.Windows.Views.PromptPaletteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:TypeWhisper.Core.Models;assembly=TypeWhisper.Core"
        Title="TypeWhisper Prompt Palette"
        Width="400"
        Height="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="True"
        FocusManager.FocusedElement="{Binding ElementName=SearchBox}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Palette ListBox Item Style -->
        <Style x:Key="PaletteListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                Margin="4,2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3D3D3D"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main container with drop shadow -->
    <Border CornerRadius="12"
            Background="#F0252526"
            BorderBrush="#40FFFFFF"
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search Box -->
            <Border Grid.Row="0"
                    Background="#2D2D2D"
                    CornerRadius="6"
                    Margin="4,4,4,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Icon -->
                    <TextBlock Grid.Column="0"
                               Text="&#x1F50D;"
                               FontSize="14"
                               Foreground="#808080"
                               VerticalAlignment="Center"
                               Margin="12,0,8,0"/>

                    <TextBox x:Name="SearchBox"
                             Grid.Column="1"
                             Foreground="#FFFFFF"
                             FontSize="14"
                             VerticalAlignment="Center"
                             CaretBrush="#FFFFFF"
                             Padding="0,10,12,10"
                             TextChanged="SearchBox_TextChanged"
                             PreviewKeyDown="SearchBox_PreviewKeyDown">
                        <TextBox.Template>
                            <ControlTemplate TargetType="TextBox">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Background="Transparent"/>
                            </ControlTemplate>
                        </TextBox.Template>
                    </TextBox>
                </Grid>
            </Border>

            <!-- Source text preview -->
            <Border x:Name="SourcePreviewBorder"
                    Grid.Row="1"
                    Background="#20FFFFFF"
                    CornerRadius="6"
                    Padding="10,6"
                    Margin="4,0,4,6"
                    Visibility="Collapsed">
                <TextBlock x:Name="SourcePreviewText"
                           Foreground="#AAAAAA"
                           FontSize="11"
                           TextTrimming="CharacterEllipsis"
                           MaxHeight="36"
                           TextWrapping="Wrap"/>
            </Border>

            <!-- Action List -->
            <ListBox x:Name="ActionListBox"
                     Grid.Row="2"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ItemContainerStyle="{StaticResource PaletteListBoxItemStyle}"
                     SelectionChanged="ActionListBox_SelectionChanged"
                     MouseDoubleClick="ActionListBox_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:PromptAction}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Icon -->
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Icon}"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>

                            <!-- Name -->
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Name}"
                                       FontSize="14"
                                       Foreground="#FFFFFF"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Empty state -->
            <TextBlock x:Name="EmptyText"
                       Grid.Row="2"
                       Text="Keine Aktionen gefunden"
                       Foreground="#808080"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="Collapsed"/>

            <!-- Processing status -->
            <Border x:Name="StatusBorder"
                    Grid.Row="3"
                    Background="#20FFFFFF"
                    CornerRadius="6"
                    Padding="10,6"
                    Margin="4,4,4,4"
                    Visibility="Collapsed">
                <TextBlock x:Name="StatusText"
                           Foreground="#CCCCCC"
                           FontSize="12"
                           HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>
