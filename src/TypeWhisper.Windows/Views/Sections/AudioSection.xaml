<UserControl x:Class="TypeWhisper.Windows.Views.Sections.AudioSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:loc="clr-namespace:TypeWhisper.Windows.Services.Localization">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/UnifiedSettingsStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="500" Margin="24">
            <TextBlock Text="{loc:Str Recording.Title}" Style="{StaticResource SectionHeaderStyle}"/>

            <!-- Microphone -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="{loc:Str Recording.Microphone}" Style="{StaticResource LabelStyle}"/>
                <ComboBox ItemsSource="{Binding Settings.Microphones}"
                          DisplayMemberPath="Name"
                          SelectedValuePath="DeviceNumber"
                          SelectedValue="{Binding Settings.SelectedMicrophoneDevice}"
                          HorizontalAlignment="Stretch"/>
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:Button Grid.Column="0" Appearance="Secondary" Content="{loc:Str Recording.Refresh}"
                               Command="{Binding Settings.RefreshMicrophonesCommand}"/>
                    <ProgressBar Grid.Column="1" Minimum="0" Maximum="1"
                                 Value="{Binding Settings.PreviewLevel, Mode=OneWay}"
                                 Height="8" Margin="12,0,0,0"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource AccentBrush}"/>
                </Grid>
            </StackPanel>

            <!-- Silence Detection -->
            <TextBlock Text="{loc:Str Recording.SilenceDetection}" Style="{StaticResource SectionHeaderStyle}"/>
            <StackPanel Margin="0,0,0,8">
                <ui:ToggleSwitch IsChecked="{Binding Settings.SilenceAutoStopEnabled}"
                                  OffContent="{loc:Str Recording.AutoStopSilence}"
                                  OnContent="{loc:Str Recording.AutoStopSilence}"/>
            </StackPanel>
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="{loc:Str Recording.Seconds}" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Settings.SilenceAutoStopSeconds}"
                         Style="{StaticResource InputTextBoxStyle}"
                         Width="100" HorizontalAlignment="Left"/>
            </StackPanel>

            <!-- Audio Ducking -->
            <TextBlock Text="{loc:Str Recording.AudioDucking}" Style="{StaticResource SectionHeaderStyle}"/>
            <StackPanel Margin="0,0,0,8">
                <ui:ToggleSwitch IsChecked="{Binding Settings.AudioDuckingEnabled}"
                                  OffContent="{loc:Str Recording.ReduceVolume}"
                                  OnContent="{loc:Str Recording.ReduceVolume}"/>
            </StackPanel>
            <StackPanel Margin="0,0,0,8"
                        Visibility="{Binding Settings.AudioDuckingEnabled, Converter={StaticResource BoolToVis}}">
                <TextBlock Style="{StaticResource LabelStyle}">
                    <Run Text="{Binding Source={x:Static loc:Loc.Instance}, Path=[Recording.VolumeLabel], Mode=OneWay}"/>
                    <Run Text="{Binding Settings.AudioDuckingLevel, StringFormat='{}{0:P0}', Mode=OneWay}"/>
                </TextBlock>
                <Slider Minimum="0" Maximum="0.5"
                        TickFrequency="0.05" IsSnapToTickEnabled="True"
                        Value="{Binding Settings.AudioDuckingLevel}"
                        HorizontalAlignment="Stretch"/>
                <TextBlock Text="{loc:Str Recording.VolumeHint}"
                           Style="{StaticResource HintStyle}"/>
            </StackPanel>

            <!-- Media Pause -->
            <TextBlock Text="{loc:Str Recording.MediaPlayback}" Style="{StaticResource SectionHeaderStyle}"/>
            <StackPanel Margin="0,0,0,8">
                <ui:ToggleSwitch IsChecked="{Binding Settings.PauseMediaDuringRecording}"
                                  OffContent="{loc:Str Recording.PausePlayback}"
                                  OnContent="{loc:Str Recording.PausePlayback}"/>
                <TextBlock Text="{loc:Str Recording.PausePlaybackHint}"
                           Style="{StaticResource HintStyle}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
